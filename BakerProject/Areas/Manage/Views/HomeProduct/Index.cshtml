
@{
    ViewData["Title"] = "Index";
}
@{
    int index = 1;
}
@model List<HomeProduct>

@if (Model != null && Model.Count > 0)
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>#</th>
                <th>Image</th>
                <th>Title</th>
                <th>Description</th>
                <th colspan="2">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@(index++)</td>
                    <td><img width="70" src="~/assets/images/@item.Image" /></td>
                    <td>@item.Title</td>
                    <td>@item.Description</td>
                    <td><a asp-controller="HomeProduct" asp-action="Edit" asp-area="Manage" asp-route-id="@item.Id" class="text-info">Edit</a></td>
                    <td><a asp-controller="HomeProduct" asp-action="Delete" asp-area="Manage" asp-route-id="@item.Id" class="text-danger">Delete</a></td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <h1 class="display-4">Home Products page is empty</h1>
}



